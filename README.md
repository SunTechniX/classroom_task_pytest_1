# 🏠 Home_26: Домашнее задание

## 🎯 Цель
Научиться создавать структуру проекта, писать тесты, запускать их разными способами и анализировать результаты.

---

## 📌 Задание 1: Создайте проект с несколькими файлами

Создайте проект `hw_pytest` со следующей структурой:

```
hw_pytest/
├── tests/
│   ├── test_calculator.py
│   └── test_string_utils.py
└── README.md
```

### В `test_calculator.py`:
- Напишите 3 функции: `add`, `subtract`, `multiply`.
- Напишите 3 теста этих функций: `test_add`, `test_subtract`, `test_multiply`.
- Добавьте один тест — `test_fail_intentionally`, который намеренно проваливается.
- Используйте маркер `@pytest.mark.math`.

### В `test_string_utils.py`:
- Напишите 2 функции: `to_upper`, `reverse_string`.
- Напишите 2 теста для них: `test_uppercase`, `test_reverse`.
- Используйте маркер `@pytest.mark.string`.

---

## 📌 Задание 2: Запуск тестов по разным критериям

Выполните следующие команды и зафиксируйте результаты:

1. Запустите только тесты из `test_calculator.py`.
2. Запустите только тесты с маркером `math`.
3. Запустите все тесты, кроме тех, что содержат слово “reverse”.
4. Запустите тесты с выводом `print()` и покажите, как это работает.

---

## 📌 Задание 3 (по желанию): Отчёт HTML

1. Установите плагин:  
   ```bash
   pip install pytest-html
   ```
2. Запустите тесты с генерацией отчёта:  
   ```bash
   pytest --html=report.html
   ```
3. Откройте `report.html` в браузере и сделайте скриншот главной страницы отчёта.
